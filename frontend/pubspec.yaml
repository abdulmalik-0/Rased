name: server_dashboard
description: AI-Powered Server Dashboard - Docker monitoring with on-demand log analysis
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  supabase_flutter: ^2.8.3
  http: ^1.2.2
  fl_chart: ^0.69.2
  flutter_markdown: ^0.7.4+3
  intl: ^0.20.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
